<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Client">

    <select id="clientList" resultType="com.lgyf.demo.bean.Client">
        select * from client where is_agent=0
    </select>

    <select id="agentArgsList" resultType="com.lgyf.demo.bean.AgentArgs">
        select client_no as agent_no,name,client_public_key as public_key,status,socket_ip from client where is_agent=1
    </select>


    <insert id="ist_client" parameterType="com.lgyf.demo.bean.Client">
          insert into client(client_no,name,phone,client_public_key,socket_ip,client_name,status,is_agent,add_time)
          values(#{client_no},#{name},#{phone},#{client_public_key},#{socket_ip},#{client_name},#{status},0,sysdate())
    </insert>
    
    <!-- 根据身份证号查询开户信息 -->
    <select id="getOpenAccountByCertNo" resultType="java.util.HashMap">
        select * from open_account where cert_no = #{cert_no} limit 1
    </select>
    
    <!-- 插入开户信息 -->
    <insert id="ist_open_account" parameterType="com.lgyf.demo.bean.OpenAccount">
        insert into open_account(
            clilent_no, name, account_no, mobile, open_status, cert_no, msg, 
            add_time, otp_order_no, business_no, ele_account_no, client_ip, 
            home_address, lbs, device_id, agreement_no, open_order_no
        ) values (
            #{clilent_no}, #{name}, #{account_no}, #{mobile}, #{open_status}, #{cert_no}, #{msg},
            #{add_time}, #{otp_order_no}, #{business_no}, #{ele_account_no}, #{client_ip},
            #{home_address}, #{lbs}, #{device_id}, #{agreement_no}, #{open_order_no}
        )
    </insert>
    
    <!-- 插入电子账户信息 -->
    <insert id="ist_ele_account" parameterType="com.lgyf.demo.bean.EleAccount">
        insert into ele_account(
            clilent_no, name, account_no, mobile, cert_no, ele_account_no, 
            agreement_no, add_date, add_time, ele_account_status
        ) values (
            #{clilent_no}, #{name}, #{account_no}, #{mobile}, #{cert_no}, #{ele_account_no},
            #{agreement_no}, #{add_date}, #{add_time}, #{ele_account_status}
        )
    </insert>


</mapper>

